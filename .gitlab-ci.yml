include:
 - project: doudous/ci-templates
   ref: main
   file: build.yaml

build:logrotate:
  needs: []
  extends: .build_container
  stage: build
  variables:
    CONTEXT: logrotate
    REPO: ghcr.io/vaskozl/logrotate
    DOCKER_AUTH_CONFIG: $GHCR_AUTH_CONFIG


build:lwp-simple:
  needs: []
  extends: .build_container
  stage: build
  variables:
    CONTEXT: lwp-simple
    REPO: ghcr.io/vaskozl/lwp-simple
    DOCKER_AUTH_CONFIG: $GHCR_AUTH_CONFIG

build:jellyfin:
  needs: []
  extends: .build_container
  stage: build
  variables:
    CONTEXT: jellyfin
    REPO: ghcr.io/vaskozl/jellyfin
    DOCKER_AUTH_CONFIG: $GHCR_AUTH_CONFIG
