# yaml-language-server: $schema=https://raw.githubusercontent.com/chainguard-dev/apko/refs/heads/main/pkg/build/types/schema.json
include: wolfi.yaml
contents:
  packages:
    - docker=28.3.3-r3 # repo=wolfi
    - docker-cli-buildx
    - sed
    - jq
    - skopeo
    - git
entrypoint:
   command: sh
vcs-url: https://github.com/docker/cli
